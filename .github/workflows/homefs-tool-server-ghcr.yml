name: Build and Push homefs-tool-server

on:
  push:
    branches: [main]
    paths:
      - "services/homefs-tool-server/**"
      - "packages/homefs-shared/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.base.json"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/homefs-tool-server/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/homefs-tool-server:${{ github.sha }}
